name: Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Upload files
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: .
